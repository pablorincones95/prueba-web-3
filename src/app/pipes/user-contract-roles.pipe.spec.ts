import { UserContractRolesPipe } from './user-contract-roles.pipe';

describe('UserContractRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserContractRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
