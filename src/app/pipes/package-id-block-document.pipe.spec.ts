import { PackageIdBlockDocumentPipe } from './package-id-block-document.pipe';

describe('PackageIdBlockDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new PackageIdBlockDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
