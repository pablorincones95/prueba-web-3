import { UserBlockDocumentPipe } from './user-block-document.pipe';

describe('UserBlockDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new UserBlockDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
