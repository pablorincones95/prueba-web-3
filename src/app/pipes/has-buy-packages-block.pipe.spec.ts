import { HasBuyPackagesBlockPipe } from './has-buy-packages-block.pipe';

describe('HasBuyPackagesBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new HasBuyPackagesBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
