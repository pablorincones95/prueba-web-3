import { PackageBlockDocumentPipe } from './package-block-document.pipe';

describe('PackageBlockDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new PackageBlockDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
