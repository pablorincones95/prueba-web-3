import { HasBuyPackagesPipe } from './has-buy-packages.pipe';

describe('HasBuyPackagesPipe', () => {
  it('create an instance', () => {
    const pipe = new HasBuyPackagesPipe();
    expect(pipe).toBeTruthy();
  });
});
