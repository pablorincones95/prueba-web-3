import { UserDocFbPipe } from './user-doc-fb.pipe';

describe('UserDocFbPipe', () => {
  it('create an instance', () => {
    const pipe = new UserDocFbPipe();
    expect(pipe).toBeTruthy();
  });
});
